{
  "name": "ready-board",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "ready.3dl.dev/board — nostr-login + owner-board discovery (ready-dbf), built with Vite + TypeScript, no runtime CDN dependencies.",
  "engines": {
    "node": ">=20.17.0"
  },
  "scripts": {
    "build": "tsc -b && vite build",
    "dev": "vite",
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "25.0.1",
    "typescript": "5.6.3",
    "vite": "5.4.21",
    "vitest": "2.1.9"
  }
}
